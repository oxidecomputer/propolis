[workspace]

members = [
  "cli",
  "client",
  "package",
  "propolis",
  "server",
  "standalone",
  "types",
]

default-members = [
  "cli",
  "client",
  "propolis",
  "server",
  "standalone",
  "types",
]

exclude = [
  "bhyve-api/header-check"
]

[profile.dev]
panic = "abort"
[profile.release]
panic = "abort"
