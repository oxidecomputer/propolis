[workspace]

members = [
  "cli",
  "client",
  "package",
  "propolis",
  "server",
  "standalone",
]

default-members = [
  "cli",
  "client",
  "propolis",
  "server",
  "standalone",
]

[profile.dev]
panic = "abort"
[profile.release]
panic = "abort"
