[workspace]

members = [
  "crates/*",
  "bin/*",
  "lib/*",
  "packaging/propolis-package",
  "phd-tests/*",
]

default-members = [
  "crates/*",
  "lib/*",
  "bin/propolis-cli",
  "bin/propolis-server",
  "bin/propolis-standalone",
  "phd-tests/*",
]

exclude = [
  "crates/*/header-check",
  "phd-tests/target",
]

[profile.dev]
panic = "abort"
[profile.release]
panic = "abort"

# Special
[profile.phd]
inherits = "dev"
panic = "unwind"
