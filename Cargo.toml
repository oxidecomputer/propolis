[workspace]

members = [
  "cli",
  "client",
  "package",
  "propolis",
  "server",
  "standalone",
]

default-members = [
  "cli",
  "client",
  "propolis",
  "server",
  "standalone",
]

exclude = [
  "bhyve-api/header-check"
]

[profile.dev]
panic = "abort"
[profile.release]
panic = "abort"

# Development
[patch."https://github.com/oxidecomputer/crucible"]
crucible = { path = "../stock/upstairs" }
